# docs/

Cross-platform documentation and media.

Put here:
- Screenshots / GIFs used by the READMes (e.g. `docs/screenshot-windows.png`).
- Design notes, architecture, and per-platform integration notes for Tor as
  they are written.

Reference an image from the root README like:

```markdown
![Tor-proxy on Windows](docs/screenshot-windows.png)
```
